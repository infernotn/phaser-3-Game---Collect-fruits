var data = {
  currentScore: 0,
  highScore: 0,
  scorePerApple: 1,
  spawnSpeed: 500,
  maxHearts: 3,
  currentHealth: 0,
};

interface gameData {
  currentScore: number;
  highScore: number;
  scorePerApple: number;
  spawnSpeed: number;
  maxHearts: number;
  currentHealth: number;
}

export default data;
